---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: aiworkloadprofiles.astra.aiinfra.io
spec:
  group: astra.aiinfra.io
  names:
    kind: AIWorkloadProfile
    listKind: AIWorkloadProfileList
    plural: aiworkloadprofiles
    singular: aiworkloadprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workloadType
      name: Type
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: string
    - jsonPath: .spec.demandShape
      name: Shape
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIWorkloadProfile is the Schema for the aiworkloadprofiles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AIWorkloadProfile.
            properties:
              demandShape:
                description: |-
                  demandShape describes the dominant AI serving pressure.
                  Examples: decode_heavy, prefill_heavy, kv_heavy, mixed.
                enum:
                - mixed
                - kv_heavy
                - prefill_heavy
                - decode_heavy
                type: string
              policy:
                description: policy describes which adjustment actions are allowed
                  for this workload.
                properties:
                  allowBorrowing:
                    description: allowBorrowing means this workload may use idle capacity
                      above its required budget.
                    type: boolean
                  allowReclaimFromLowerPriority:
                    description: allowReclaimFromLowerPriority means this workload
                      can reclaim capacity from lower-priority workloads.
                    type: boolean
                  evictable:
                    description: evictable means this workload can be terminated or
                      moved as a last-resort action.
                    type: boolean
                  pauseable:
                    description: pauseable means the workload can be paused and resumed
                      by the agent/runtime.
                    type: boolean
                  throttleable:
                    description: throttleable means the scheduler may ask the agent/runtime
                      to reduce concurrency or token budget.
                    type: boolean
                type: object
              priority:
                description: |-
                  priority is the business priority used by the policy engine.
                  Examples: high, medium, low.
                type: string
              resources:
                description: resources describes hard required resources and soft
                  preferred resources.
                properties:
                  preferred:
                    description: preferred is the target resource budget used for
                      scoring and elastic growth.
                    properties:
                      decodeTokensPerSecond:
                        format: int32
                        type: integer
                      gpuCount:
                        format: int32
                        type: integer
                      gpuMemoryGiB:
                        format: int32
                        type: integer
                      kvCacheGiB:
                        format: int32
                        type: integer
                      prefillTokensPerSecond:
                        format: int32
                        type: integer
                      totalTokensPerSecond:
                        format: int32
                        type: integer
                    type: object
                  required:
                    description: required is the minimum resource budget needed to
                      place this workload.
                    properties:
                      decodeTokensPerSecond:
                        format: int32
                        type: integer
                      gpuCount:
                        format: int32
                        type: integer
                      gpuMemoryGiB:
                        format: int32
                        type: integer
                      kvCacheGiB:
                        format: int32
                        type: integer
                      prefillTokensPerSecond:
                        format: int32
                        type: integer
                      totalTokensPerSecond:
                        format: int32
                        type: integer
                    type: object
                type: object
              slo:
                description: slo describes service-level objectives or offline deadlines.
                properties:
                  deadlineHours:
                    description: deadlineHours is mainly used by delay-tolerant offline
                      jobs.
                    format: int32
                    type: integer
                  latencyP99Ms:
                    description: latencyP99Ms is the p99 end-to-end latency target.
                    format: int32
                    type: integer
                  ttftP95Ms:
                    description: ttftP95Ms is the p95 time-to-first-token target for
                      online LLM serving.
                    format: int32
                    type: integer
                type: object
              timeWindows:
                description: timeWindows describe business peak windows that should
                  affect scheduling.
                items:
                  description: TimeWindow describes a repeating daily business window.
                  properties:
                    end:
                      description: end is a local wall-clock time in HH:MM format.
                        It may be earlier than start for overnight windows.
                      type: string
                    name:
                      type: string
                    start:
                      description: start is a local wall-clock time in HH:MM format.
                      type: string
                    timezone:
                      description: timezone is an IANA timezone name, such as America/New_York.
                      type: string
                  required:
                  - end
                  - name
                  - start
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              workloadRef:
                description: workloadRef points to the Kubernetes workload represented
                  by this profile.
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    description: namespace is optional because some referenced objects
                      can be cluster-scoped.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              workloadType:
                description: |-
                  workloadType describes whether the workload is online, offline, or hybrid.
                  Examples: online, offline, hybrid.
                type: string
            required:
            - priority
            - workloadRef
            - workloadType
            type: object
          status:
            description: status defines the observed state of AIWorkloadProfile.
            properties:
              conditions:
                description: conditions represent the current state of the AIWorkloadProfile
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentWindow:
                description: currentWindow is the active time window name, if any.
                type: string
              phase:
                description: phase is the high-level lifecycle phase observed by the
                  scheduler.
                type: string
              sloRisk:
                description: sloRisk is a coarse risk level inferred from metrics,
                  such as low, medium, or high.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
