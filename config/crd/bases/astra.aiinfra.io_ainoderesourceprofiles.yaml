---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ainoderesourceprofiles.astra.aiinfra.io
spec:
  group: astra.aiinfra.io
  names:
    kind: AINodeResourceProfile
    listKind: AINodeResourceProfileList
    plural: ainoderesourceprofiles
    singular: ainoderesourceprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .spec.backendType
      name: Backend
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.available.gpuCount
      name: GPU
      type: integer
    - jsonPath: .status.available.kvCacheGiB
      name: KVCache
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AINodeResourceProfile is the Schema for the ainoderesourceprofiles
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AINodeResourceProfile
            properties:
              backendType:
                description: |-
                  backendType identifies the node-side backend that reports and applies resources.
                  Examples: fake, nvidia, runtime, cri.
                type: string
              capabilities:
                description: capabilities describes which node-side adjustment actions
                  are supported.
                properties:
                  borrowing:
                    type: boolean
                  eviction:
                    type: boolean
                  migResize:
                    type: boolean
                  pauseResume:
                    type: boolean
                  reclaim:
                    type: boolean
                  sharing:
                    type: boolean
                  throttling:
                    type: boolean
                type: object
              gpus:
                description: gpus describes static GPU device capability from the
                  scheduler's view.
                items:
                  description: GPUDeviceSpec describes static capability of one GPU
                    device.
                  properties:
                    computeScore:
                      description: computeScore is a normalized compute capacity score
                        used by the scheduler.
                      format: int32
                      type: integer
                    id:
                      description: id is a stable local identifier used by Astra.
                      type: string
                    memoryGiB:
                      description: memoryGiB is the physical GPU memory size.
                      format: int32
                      type: integer
                    migEnabled:
                      description: migEnabled indicates whether this GPU is currently
                        managed as MIG-capable.
                      type: boolean
                    product:
                      description: product is the GPU product name, such as A100,
                        H100, L40S, or fake-a100.
                      type: string
                    uuid:
                      description: uuid is the vendor or backend reported GPU UUID.
                      type: string
                  required:
                  - id
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              nodeName:
                description: nodeName is the Kubernetes node represented by this profile.
                type: string
              runtime:
                description: runtime describes AI runtime capacity and supported serving
                  features.
                properties:
                  capacity:
                    description: capacity describes the runtime-level schedulable
                      budget.
                    properties:
                      kvCacheGiB:
                        description: kvCacheGiB is the KV-cache budget exposed to
                          the scheduler.
                        format: int32
                        type: integer
                      maxBatchTokens:
                        description: maxBatchTokens is the maximum token count the
                          runtime can batch.
                        format: int32
                        type: integer
                      maxConcurrentRequests:
                        description: maxConcurrentRequests is the approximate request
                          concurrency limit.
                        format: int32
                        type: integer
                      tokenThroughput:
                        description: tokenThroughput describes prefill/decode token
                          budget.
                        properties:
                          decodeTokensPerSecond:
                            format: int32
                            type: integer
                          prefillTokensPerSecond:
                            format: int32
                            type: integer
                          totalTokensPerSecond:
                            format: int32
                            type: integer
                        type: object
                    type: object
                  supports:
                    description: supports lists runtime features usable by the scheduler
                      and agent.
                    properties:
                      continuousBatching:
                        type: boolean
                      kvCache:
                        type: boolean
                      pauseResume:
                        type: boolean
                      prefixCache:
                        type: boolean
                      requestThrottling:
                        type: boolean
                      tokenThroughput:
                        type: boolean
                    type: object
                  type:
                    description: type identifies the runtime family, such as fake,
                      vllm, sglang, tgi, or tensorrt-llm.
                    type: string
                type: object
            required:
            - nodeName
            type: object
          status:
            description: status defines the observed state of AINodeResourceProfile
            properties:
              allocatable:
                description: allocatable summarizes schedulable capacity.
                properties:
                  decodeTokensPerSecond:
                    format: int32
                    type: integer
                  gpuCount:
                    format: int32
                    type: integer
                  gpuMemoryGiB:
                    format: int32
                    type: integer
                  kvCacheGiB:
                    format: int32
                    type: integer
                  prefillTokensPerSecond:
                    format: int32
                    type: integer
                  totalTokensPerSecond:
                    format: int32
                    type: integer
                type: object
              allocated:
                description: allocated summarizes currently assigned resources.
                properties:
                  decodeTokensPerSecond:
                    format: int32
                    type: integer
                  gpuCount:
                    format: int32
                    type: integer
                  gpuMemoryGiB:
                    format: int32
                    type: integer
                  kvCacheGiB:
                    format: int32
                    type: integer
                  prefillTokensPerSecond:
                    format: int32
                    type: integer
                  totalTokensPerSecond:
                    format: int32
                    type: integer
                type: object
              allocations:
                description: allocations lists workload allocations currently applied
                  on this node.
                items:
                  description: NodeAllocationStatus describes one workload allocation
                    currently applied on the node.
                  properties:
                    actions:
                      description: AllocationActions describes what the scheduler/agent
                        may do to this allocation.
                      properties:
                        evictable:
                          type: boolean
                        pauseable:
                          type: boolean
                        reclaimable:
                          type: boolean
                        throttleable:
                          type: boolean
                      type: object
                    assignedGPU:
                      description: AssignedGPU identifies the physical GPU or MIG
                        slice assigned to an allocation.
                      properties:
                        gpuID:
                          type: string
                        migSliceID:
                          type: string
                      type: object
                    demandShape:
                      description: demandShape describes whether the workload is prefill-heavy,
                        decode-heavy, kv-heavy, or mixed.
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    phase:
                      type: string
                    priority:
                      type: string
                    resources:
                      description: ResourceSummary summarizes GPU and runtime resources
                        at node level.
                      properties:
                        decodeTokensPerSecond:
                          format: int32
                          type: integer
                        gpuCount:
                          format: int32
                          type: integer
                        gpuMemoryGiB:
                          format: int32
                          type: integer
                        kvCacheGiB:
                          format: int32
                          type: integer
                        prefillTokensPerSecond:
                          format: int32
                          type: integer
                        totalTokensPerSecond:
                          format: int32
                          type: integer
                      type: object
                    workloadName:
                      type: string
                    workloadType:
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              available:
                description: available summarizes remaining resources.
                properties:
                  decodeTokensPerSecond:
                    format: int32
                    type: integer
                  gpuCount:
                    format: int32
                    type: integer
                  gpuMemoryGiB:
                    format: int32
                    type: integer
                  kvCacheGiB:
                    format: int32
                    type: integer
                  prefillTokensPerSecond:
                    format: int32
                    type: integer
                  totalTokensPerSecond:
                    format: int32
                    type: integer
                type: object
              conditions:
                description: conditions represent the current state of the AINodeResourceProfile
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              gpus:
                description: gpus describes current GPU and MIG slice state.
                items:
                  description: GPUDeviceStatus describes observed GPU state.
                  properties:
                    allocated:
                      type: boolean
                    allocatedTo:
                      description: AllocationRef identifies the workload allocation
                        using a resource.
                      properties:
                        allocationName:
                          type: string
                        namespace:
                          type: string
                        workloadName:
                          type: string
                      type: object
                    id:
                      type: string
                    migEnabled:
                      type: boolean
                    migSlices:
                      description: migSlices describes concrete MIG instances available
                        on this GPU.
                      items:
                        description: MIGSliceStatus describes one concrete schedulable
                          MIG slice.
                        properties:
                          allocatable:
                            type: boolean
                          allocated:
                            type: boolean
                          allocatedTo:
                            description: AllocationRef identifies the workload allocation
                              using a resource.
                            properties:
                              allocationName:
                                type: string
                              namespace:
                                type: string
                              workloadName:
                                type: string
                            type: object
                          computeSlices:
                            description: computeSlices is the relative compute partition
                              count represented by this slice.
                            format: int32
                            type: integer
                          id:
                            type: string
                          memoryGiB:
                            format: int32
                            type: integer
                          profile:
                            description: profile is the MIG slice shape, such as 1g.10gb
                              or 2g.20gb.
                            type: string
                          uuid:
                            type: string
                        required:
                        - id
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - id
                      x-kubernetes-list-type: map
                    uuid:
                      type: string
                  required:
                  - id
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              observedAt:
                description: observedAt is the last time this profile was refreshed
                  by the agent.
                format: date-time
                type: string
              phase:
                description: phase is the high-level readiness phase reported by the
                  node agent.
                type: string
              runtime:
                description: runtime describes runtime pressure and observed serving
                  signals.
                properties:
                  pressure:
                    description: RuntimePressure contains percent-based pressure and
                      SLO-related runtime signals.
                    properties:
                      decodeUtilizationPercent:
                        format: int32
                        type: integer
                      gpuMemoryUtilizationPercent:
                        format: int32
                        type: integer
                      gpuUtilizationPercent:
                        format: int32
                        type: integer
                      kvCacheUtilizationPercent:
                        format: int32
                        type: integer
                      latencyP99Ms:
                        format: int32
                        type: integer
                      prefillUtilizationPercent:
                        format: int32
                        type: integer
                      queueDepth:
                        format: int32
                        type: integer
                      sloRisk:
                        description: sloRisk is a coarse risk level, such as low,
                          medium, or high.
                        type: string
                      tpotP95Ms:
                        format: int32
                        type: integer
                      ttftP95Ms:
                        format: int32
                        type: integer
                    type: object
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
